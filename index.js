/**
 * @format
 */

import { AppRegistry } from 'react-native';
import EmporioAzuki from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => EmporioAzuki);
